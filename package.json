{
  "name": "api-gen",
  "version": "0.1.0",
  "description": "Toolset for REST Api creation",
  "main": "dist/index.js",
  "author": "Nicolas Schmidt",
  "license": "BSD-2-Clause",
  "dependencies": {
    "cross-fetch": "^3.1.6",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "source-map-support": "^0.5.21",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "start": "node dist/index.js",
    "start-test": "node -r source-map-support/register dist/test.js",
    "test": "yarn build && yarn start-test",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.client.json"
  },
  "type": "module"
}
